# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'  # Change this to match your Flutter minimum iOS version

# CocoaPods analytics sends network requests, disable it for faster builds
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

# Define the Flutter root directory
flutter_application_path = File.expand_path('..', __FILE__)

# Ensure Flutter is installed
load File.join(flutter_application_path, 'Flutter', 'podhelper.rb')

target 'Runner' do
  use_frameworks!  # Enable dynamic frameworks (needed for some plugins)

  # Install Flutter dependencies
  install_all_flutter_pods(flutter_application_path)

  target 'RunnerTests' do
    inherit! :search_paths
  end
end
